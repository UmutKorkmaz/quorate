{
  "name": "@quorate/github-action",
  "version": "0.9.0",
  "main": "./dist/index.js",
  "private": true,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && esbuild src/index.ts --bundle --platform=node --format=cjs --target=node20 --outfile=dist/index.js",
    "test": "cd ../.. && vitest run packages/github-action/test"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "@quorate/core": "0.9.0"
  }
}
