diff --git a/programs/swap/src/lib.rs b/programs/swap/src/lib.rs
--- a/programs/swap/src/lib.rs
+++ b/programs/swap/src/lib.rs
@@ -16,6 +16,8 @@ pub mod swap {
     pub fn swap_tokens(ctx: Context<SwapTokens>, amount: u64) -> Result<()> {
         let token_info = ctx.accounts.token_account.to_account_info();
+        let token_data = spl_token::state::Account::unpack(&token_info.data.borrow())?;
+        let balance = token_data.amount;
         Ok(())
     }
 }
