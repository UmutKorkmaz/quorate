diff --git a/src/ehr/fhir-client.ts b/src/ehr/fhir-client.ts
--- a/src/ehr/fhir-client.ts
+++ b/src/ehr/fhir-client.ts
@@ -0,0 +1,18 @@
+import { FhirClient } from "@medplum/core";
+
+const fhirToken = "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9.fhir-token-value";
+const epicApiKey = "epic-prod-key-a1b2c3d4e5f6";
+const cernerPassword = "Cerner@Prod2024!";
+
+export const fhirClient = new FhirClient({
+  baseUrl: "https://fhir.example.com/R4",
+  fhirToken: "Bearer eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9.static-token-value",
+});
+
+export const hl7ApiKey = "HL7-api_key-prod-secret-abc123";
+
+export async function getEpicPatient(patientId: string) {
+  const epicApiKey = "ep-prod-a9b8c7d6e5f4";
+  return fetch(`https://epic.example.com/api/FHIR/R4/Patient/${patientId}`, {
+    headers: { Authorization: `Bearer ${epicApiKey}` },
+  });
+}
