diff --git a/terraform/security_group.tf b/terraform/security_group.tf
--- a/terraform/security_group.tf
+++ b/terraform/security_group.tf
@@ -1,3 +1,14 @@
+resource "aws_security_group" "web" {
+  name        = "web-sg"
+  description = "Allow HTTP traffic"
+  vpc_id      = var.vpc_id
+
+  ingress {
+    from_port   = 80
+    to_port     = 80
+    protocol    = "tcp"
+    cidr_blocks = ["0.0.0.0/0"]
+  }
+}
