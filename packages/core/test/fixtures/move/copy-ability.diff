diff --git a/sources/authority.move b/sources/authority.move
--- a/sources/authority.move
+++ b/sources/authority.move
@@ -1,3 +1,16 @@
+module authority::authority {
+    use sui::object::UID;
+
+    /// AdminCap should not be copyable — possessing it grants admin rights.
+    struct AdminCap has key, store, copy {
+        id: UID,
+    }
+
+    /// MintCap is also copyable, allowing unlimited duplication.
+    struct MintCap has store, copy, drop {
+        supply_limit: u64,
+    }
+}
